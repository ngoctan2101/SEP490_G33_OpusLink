@page
@model OpusLink.User.Hosted.Pages.Login_Register.RegisterModel
@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register Form | Opuslink Team</title>
    <link rel="stylesheet" href="~/css/Register.css">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
</head>
<body>
    <div class="wrapper">
        <form method="post">
            <h1>Đăng ký | Opuslink</h1>
            <div class="input-box">
                <input type="text" placeholder="Nhập tên đăng nhập" id="UserName" name="Username" required>
                <i class='bx bxs-user-pin'></i>
            </div>
            <div class="input-box">
                <input type="email" placeholder="Nhập email" id="Email" name="Email" required>
                <i class='bx bxs-envelope'></i>
            </div>
            <div class="input-box">
                <input type="password" placeholder="Nhập mật khẩu" id="Password" name="Password" required>
                <i class='bx bxs-lock-alt'></i>
            </div>
            <div class="input-box">
                <input type="password" placeholder="Nhập lại mật khẩu" id="ConfirmPassword" name="ConfirmPassword" required>
                <i class='bx bxs-lock-alt'></i>
            </div>
            @if (ViewData["Success"] != null)
            {
                <div class="alert alert-success" role="alert">
                    @ViewData["Success"]
                </div>
            }
            @if (ViewData["Error"] != null)
            {
                <div class="alert alert-danger" role="alert">
                    @ViewData["Error"]
                </div>
            }
            @* @if (Model.IsRegistered)
            {
                <div class="modal" tabindex="-1" role="dialog" id="successModal">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title">Đăng ký thành công!</h5>
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                    <span aria-hidden="true">&times;</span>
                                </button>
                            </div>
                            <div class="modal-body">
                                <p>Thông báo thành công khi đăng ký.</p>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-primary" data-dismiss="modal">OK</button>
                            </div>
                        </div>
                    </div>
                </div>
            } *@
            <button type="submit" class="btn-register">Đăng ký tài khoản</button>
            <div class="login-link">
                <p>Đã có tài khoản? <a asp-page="/User/Login">Đăng nhập</a></p>
            </div>
        </form>
    </div>
    @* <script>
        // Hiển thị modal khi đăng ký thành công
        $(document).ready(function () {
            $('#successModal').modal('show');
        });
    </script> *@
</body>
</html>
